machine:
    node:
        version: 6.1.0

dependencies:
    override:
        - npm install --dev

test:
    override: 
        - npm run test-once

deployment:
    staging:
        branch: master
        heroku:
            appname: benstutorial